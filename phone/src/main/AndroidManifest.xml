<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.github.magloft.mlx.phone">
  <uses-permission android:name="android.permission.CALL_PHONE" />
  <application android:allowBackup="true">
    <provider
        android:authorities="${applicationId}.com.github.magloft.mlx.phone"
        android:exported="false"
        android:enabled="true"
        android:name=".Phone" />
  </application>
</manifest>
